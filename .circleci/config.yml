version: 2.1

jobs:
  lint:
    docker:
      - image: mambaorg/micromamba
    steps:
      - checkout
      - run: "mamba install pylint"
      - run: "pylint phylofisher --exit-zero"
  
  test:
    docker:
      - image: mambaorg/micromamba
    steps:
      - checkout
      - run: "mamba install -y -c bioconda phylofisher pytest"
      - run: "mamba remove --force -y phylofisher"
      - run: "pip install --no-deps ."
      - run: "pytest"

workflows:
  commit-status:
    jobs:
      - lint
      - test
