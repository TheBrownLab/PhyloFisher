version: 2.1

jobs:
  # Run linting
  lint:
    docker:
      - image: continuumio/anaconda3
    steps:
      - checkout
      - run: "conda install pylint"
      - run: "pylint phylofisher --exit-zero"
  # Run tests
  test:
    docker:
      - image: continuumio/anaconda3
    steps:
      - checkout
      - run: "conda install -y -c conda-forge mamba=0.15.2"
      - run: "mamba install -y -c conda-forge -c bioconda phylofisher pytest python=3.7.10"
      - run: "conda remove -y --force phylofisher"
      - run: "pip install --no-deps ."
      - run: "pytest"

# Workflows
workflows:
  commit-status:
    jobs:
      - lint
      - test
